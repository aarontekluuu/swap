# swap landing page

Static single-page site for Swap. The landing page is served directly from `index.html` in the repo root; no build step is required.

## Quick start
- Open `index.html` in your browser to preview locally.

## Deployment (Vercel)
- Project: https://vercel.com/ateklu-5492s-projects/swap
- Framework preset: Other; no build command; output directory is the repo root.
- Vercel auto-deploys on pushes to `main`. If a deploy doesn’t start, click Redeploy in Vercel or push a tiny change to retrigger.
